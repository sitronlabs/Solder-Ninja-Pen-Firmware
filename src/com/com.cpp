/* Self header */
#include "com.h"

/* Project code */
#include "../app/app.h"

int com_task(void) {

    /* Process incoming orders */
    // element_heating_set(false);
    // element_temperature_target_get();
    // element_temperature_target_set();
    // element_temperature_mesured_get();
    // app_unlock();
    // app_lock();
    // menu is going to be something else

    /* Return success */
    return 0;
}
