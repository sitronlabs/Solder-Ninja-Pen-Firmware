#ifndef COM_H
#define COM_H

/**
 * Hardware version get
 * Firmware version get
 * Settings get all
 * Settings set
 * App_heating_turn_on
 * app.lock -> app_lock(COM);
 * app.unlock -> app_unlock(COM);
 */
int com_task(void);

#endif
